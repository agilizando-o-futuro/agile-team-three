const nomeDoUtilizador = "Webert"; // Variável que não pode ser alterada
let pontosNoJogo = 10; // Variável que pode ser alterada
var nivelDeDificuldade = "Médio"; // Variável que pode ser alterada, mas não é recomendada para uso moderno

let x = 5; // Variável que pode ser alterada
let y = 10; // Variável que pode ser alterada

// Função para somar dois números
//jsskf
function somar(x, y) {
    return x + y;
}

/* Função para subtrair dois números fdjfdkf
*/
function subtrair(x, y) {
    return x - y;
}